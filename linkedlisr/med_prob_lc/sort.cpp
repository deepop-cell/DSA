#include<iostream>
struct node{
    int data;
    struct node* next;
};
struct node* sortlist(struct node* head){
    struct node* i=head;
    struct node* j=head;
    while(i!=NULL){
        i=i->next;
        while(j!=NULL){
            
        }
    }
}